 "use client"; import {useQuizStore} from "../../store/useQuizStore"; export default function Quiz(){const {questions,currentIndex,selectAnswer,next,submit}=useQuizStore(); const q=questions[currentIndex]; if(!q)return null; return <div className="card bg-base-100 p-4 shadow"><h2>{q.question}</h2>{q.options.map((o,i)=><button key={i} className="btn btn-outline my-1" onClick={()=>selectAnswer(i)}>{o}</button>)}<button className="btn btn-primary mt-2" onClick={currentIndex===questions.length-1?submit:next}>Next</button></div>} 