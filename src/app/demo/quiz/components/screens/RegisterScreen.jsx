 "use client"; import {useState} from "react"; import {useQuizStore} from "../../store/useQuizStore"; export default function Register(){const start=useQuizStore(s=>s.startQuiz); const [n,setN]=useState(""); return <div className="card bg-base-100 p-4 shadow"><input className="input input-bordered mb-2" placeholder="Name" onChange={e=>setN(e.target.value)}/><button className="btn btn-primary" onClick={()=>start({name:n})}>Start</button></div>} 